# Build the CPP API
OPTION(BUILD_CPP_API "Build CPP API" ON)
IF (BUILD_CPP_API)
	ADD_SUBDIRECTORY(cpp)
ENDIF (BUILD_CPP_API)

# Install the Python API
OPTION(INSTALL_PYTHON_API "Expose Python API" ON)
IF (INSTALL_PYTHON_API)
	ADD_SUBDIRECTORY(python)
ENDIF (INSTALL_PYTHON_API)

